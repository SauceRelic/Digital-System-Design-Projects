library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

entity elevator_fsm_reset is
generic(
  FMAX: natural := 60;
  FMAX_BIT: natural := natural(ceil(
);
port(
  clk: in std_logic;
  rstb: in std_logic;
  fdest: in std_logic_vector((FMAX - 1) downto 0);
  